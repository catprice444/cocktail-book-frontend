import React from 'react';

const Cocktail = (props) => {
    console.log(props)
    // let alcohol = props.alcohols.filter(alcohol => alcohol.id == props.match.params.id)[0]
    return(
        
        <div>
        
        {/* {this.props.alcohols.name} */}
cocktail        
        </div>
    )
}

export default Cocktail